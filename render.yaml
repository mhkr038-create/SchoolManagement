databases:
  - name: school-erp-db
    databaseName: school_management
    user: school_admin
    plan: free
    region: oregon

services:
  # 1. Backend NestJS Web Service
  - type: web
    name: school-erp-api
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm i -g pnpm@11.21.0 && pnpm install --no-frozen-lockfile && pnpm --filter @school/types build && pnpm --filter api db:generate && pnpm --filter api build
    startCommand: pnpm --filter api exec prisma db push --accept-data-loss && (pnpm --filter api db:seed || true) && node apps/api/dist/main.js
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: school-erp-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRATION
        value: 7d

  # 2. Frontend React Vite Static Site
  - type: web
    name: school-erp-web
    runtime: static
    buildCommand: npm i -g pnpm@11.21.0 && pnpm install --no-frozen-lockfile && pnpm --filter @school/types build && pnpm --filter web build
    staticPublishPath: apps/web/dist
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
      - key: VITE_API_URL
        value: https://school-erp-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html